{% extends "ProjectBundle:Admin:layout.html.twig" %}

{% block containerUsers %}
    <div class="col-sm-9 col-sm-offset-3 col-md-10 col-md-offset-2 main">
        <h2 class="sub-header">Ошибка!</h2>
        {% if error is defined %}
            <p>{{error}}</p>
        {% endif %}
    </div>
{% endblock %}